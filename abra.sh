# abra.sh — recipe hooks for foodsharing-cms.
#
# Nothing special is required to run this recipe; this file exists because abra
# sources it during deploy. Add helper functions here if needed, e.g. to run a
# one-off command inside the running container:
#
#   abra app run <app> app -- npm run seed   # force a re-seed (overwrites!)
